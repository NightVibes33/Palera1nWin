# Onboarding v3 pull-request validation

This marker forces the corrected master source through pull-request CI so the managed tests, virtual iPad harness, native toolchain, package build, Quick Actions UI smoke, and full onboarding-window smoke are all visible before distribution.
