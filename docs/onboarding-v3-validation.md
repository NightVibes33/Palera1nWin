# Onboarding v3 validation

CI must compile the v3 first-run startup hook and run the packaged UI smoke that opens both the DarkSword Quick Actions downgrade view and the full four-section onboarding window.
