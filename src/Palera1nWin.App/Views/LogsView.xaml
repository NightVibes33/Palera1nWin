<UserControl x:Class="Palera1nWin.App.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
  <Grid Margin="28,20,28,28" DataContext="{Binding Logs}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,14">
      <StackPanel>
        <TextBlock Text="Logs" Style="{StaticResource Text.Title}"/>
        <TextBlock Style="{StaticResource Text.Subtitle}" Margin="0,6,0,0"
                   Text="Full session log from jailbreak, setup, and release operations."/>
      </StackPanel>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
        <ui:Button Content="Open folder" Command="{Binding OpenFolderCommand}" Margin="0,0,8,0"/>
        <ui:Button Content="Clear" Appearance="Transparent" Command="{Binding ClearCommand}"/>
      </StackPanel>
    </Grid>

    <ui:Card Grid.Row="1" VerticalAlignment="Stretch">
      <ListBox ItemsSource="{Binding LogService.Lines}"
               ItemContainerStyle="{StaticResource BareListBoxItem}"
               ScrollViewer.HorizontalScrollBarVisibility="Disabled"
               FontFamily="Consolas, Cascadia Mono, Segoe UI" FontSize="11.5"/>
    </ui:Card>
  </Grid>
</UserControl>
